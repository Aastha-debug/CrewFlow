{
  "name": "crewflow-monorepo",
  "version": "1.0.0",
  "description": "Root package.json for Railway deployment",
  "scripts": {
    "install": "cd backend && npm install && cd ../frontend && npm install",
    "build": "cd frontend && npm run build",
    "start": "cd backend && node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
